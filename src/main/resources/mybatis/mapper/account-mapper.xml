<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.newdeal.ledger.cardaccount.mapper.AccountMapper">

    <!-- 이메일로 모든 계정 정보를 조회하는 SQL 쿼리 -->
    <select id="findAllByEmail" resultType="com.newdeal.ledger.cardaccount.dto.AccountDto" parameterType="String">
        SELECT
            ano,
            email,
            name,
            cname,
            type,
            imgUrl,
            memo,
            amount,
            isExcept
        FROM
            account
        WHERE
            email = #{email}
    </select>

    <insert id="createAccount" parameterType="map">
        INSERT INTO account (email, name, cname, type, imgUrl, memo, amount)
        VALUES (#{email}, #{request.name}, #{request.cname}, #{request.type}, #{request.imgUrl}, #{request.memo},
                #{request.amount})
    </insert>

</mapper>
